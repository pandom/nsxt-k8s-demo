apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: multicast-demo-policy
spec:
  podSelector:
    matchLabels:
      app: multicast-demo
  policyTypes:
  - Egress
  egress:
  - to:
    - ipBlock:
        cidr:
	- 224.0.55.0/24
        - 224.0.0.22/32