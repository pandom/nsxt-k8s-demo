apiVersion: v1
kind: Service
metadata:
  name: nginx-test
  namespace: ncp-feature
  labels:
    feature: test 
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: tcp
  selector:
    feature: test
  type: LoadBalancer
